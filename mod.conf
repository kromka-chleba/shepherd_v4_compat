name = shepherd_v4_compat
description = Compatibility layer that re-assigns mapchunk labels by reading mapblocks from SQL and converting to node positions
depends = mapchunk_shepherd
optional_depends = nodes_nature 